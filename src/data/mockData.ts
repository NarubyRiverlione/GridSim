/**
 * Mock data entry point for Phase 0 UI testing
 * By default, exports simple mock data for clean development experience.
 *
 * To use complex mock data (e.g., in E2E tests), add query parameter: ?mockdata=e2e
 */

// Re-export simple mock data by default
export * from './mockdata.simple'
